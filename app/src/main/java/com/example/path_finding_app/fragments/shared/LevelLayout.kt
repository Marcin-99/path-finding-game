package com.example.path_finding_app.fragments.shared

class LevelLayout (startNode: IntArray, finishNode: IntArray, walls: Array<IntArray>) {
    var startNode = startNode
    var finishNode = finishNode
    var walls = walls
}